import { UserspecificTicketsPipe } from './userspecific-tickets.pipe';

describe('UserspecificTicketsPipe', () => {
  it('create an instance', () => {
    const pipe = new UserspecificTicketsPipe();
    expect(pipe).toBeTruthy();
  });
});
